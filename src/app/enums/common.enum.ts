const STATUS_ENUM = {
  PENDING: 'PENDING',
  APPROVED: 'APPROVED',
  REJECTED: 'REJECTED',
  BANNED: 'BANNED',
};

const ROLE_ENUM = {
  DEFAULT: 'DEFAULT',
  ADMIN: 'ADMIN',
  TAXI: 'TAXI',
};



export { STATUS_ENUM, ROLE_ENUM };
